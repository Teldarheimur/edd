include("std.ðs");

fn empty() unit {
    {}
}

export
fn add(a: i32, b: i32) i32 {
    a + b
}

export fn main() i16 {
    let a: i32 = 4;
    let b: i32 = 5;
    puti32(a + b);
    puts("\n");
    puti32(a - b);
    puts("\n");

    0
}
