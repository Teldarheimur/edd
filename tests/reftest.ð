const kage: i32 = 10;
static mjav: i32 = 15;

static reffer_x: i32 = 0;

fn reffer(x: i32) *i32 {
    reffer_x = x;
    &reffer_x
}

fn main() unit {
    mjav = kage + mjav;
    let hest = reffer(9);
    putint(mjav - 4);

    putint(*hest);
    *hest = 42;
    putint(*hest);
}
