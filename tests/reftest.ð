let reffer: fn(i32) *i32 = fn(x_) {
    // This will be undefined behaviour in the future, but RN it tests pointers p well
    var x = x_;
    &x
};

let hest: *i32 = reffer(9);

print(*hest);
*hest = 42;
print(*hest);
